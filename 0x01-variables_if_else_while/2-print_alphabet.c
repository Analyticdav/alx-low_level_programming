#include <stdio.h>
/**
 * main - This program outputs values lower case of a to z
 * Return: 0 (success)
 */
int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	return (0);
}
