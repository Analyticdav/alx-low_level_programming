#include "main.h"

/**
 * _islower - chechecks for lower case alphabests.
 * @c: The character to check
 * Return: 1 for lower case and 0 for anything else
 */

int _islower(int c)
{

	if (c >= 97 && c <= 122)
	{
	return (1);
	}
	return (1);
}
