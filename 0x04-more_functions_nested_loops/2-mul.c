#include "main.h"
#include <stdio.h>

/**
 * mul - this multiplies ntwo integers
 * @x: the firstt integer
 * @y: the second integer
 * Return: o (success)
 */

int mul(int x, int y)
{
int result = x * y;
return (result);
}
