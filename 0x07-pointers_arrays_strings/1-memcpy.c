#include "main.h"
/**
 * _memset - The function
 * @dest: destination
 * @src: The source
 * Return: result to dest
 */
char *_memset(char *s, char b, unsigned int n)
{
unsigned int m = 0;

while (m < n)
{
dest[m] = src[m];
m++;
}
return (dest);
}
