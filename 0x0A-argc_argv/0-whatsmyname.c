#include "main.h"
#include <stdio.h>

/**
 * main - The function
 * @argc: count for the arguments
 * @argv: argument vection to the function
 * Return: 0 is the success
 */
int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
